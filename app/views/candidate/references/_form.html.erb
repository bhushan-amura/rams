<%= form_for(@candidate_reference, url: required_url) do |f| %>
  <% if @candidate_reference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@candidate_reference.errors.count, "error") %> prohibited this candidate_reference from being saved:</h2>

      <ul>
      <% @candidate_reference.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <table>
    <tr>
      <th><%= f.label :name %></th>
      <td><%= f.text_field :name %></td>
    </tr>
    <tr>
      <th><%= f.label :email %></th>
      <td><%= f.text_field :email %></td>
    </tr>
    <tr>
      <th><%= f.label :contact %></th>
      <td><%= f.text_field :contact %></td>
    </tr>
  </table>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
