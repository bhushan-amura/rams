<h1>Editing Qualification</h1>

<%= render 'form', required_url: self.send(qualification_path,qualification_path_params(@qualification))%>

<%= link_to 'Show', self.send(qualification_path,qualification_path_params(@qualification)) %> |
<%= link_to 'Back', self.send(qualification_path('index')) %>
